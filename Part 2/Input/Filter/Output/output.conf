output {
  file {
    path => "/Users/Kunal.Agarwala/Documents/GitHub/HWC-PT2/Log_Folder_2/dummylog2.log"   # Specify the output log folder and filename
  }
   elasticsearch {
    hosts => ["localhost:9200"]
    index => "your_index"
    # Additional Elasticsearch output configurations if needed
  }
}